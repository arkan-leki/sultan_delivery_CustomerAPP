class Food {
  int id;
  String image;
  String price;
  String disprice;
  String title;
  String subtitle;
  String detiles;

  Food(this.id, this.image, this.price, this.disprice, this.title,
      this.subtitle, this.detiles);
}
